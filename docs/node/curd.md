#### 1.如何使用SQL语句实现增加、修改和删除？

`INSERT INTO 表明 (列1,...) VALUES (值1,...)`

```sql
INSERT INTO `Articles` (`title`, `content`)
VALUES ('静夜思', '床前明月光，疑是地上霜。')
```

#### 2.SQL语句的语法规则

**大小写**：这条SQL语句，都使用了大写字母改为小写也是一样可以正确运行的，只是大写的更规范一些而已。

**反引号**： 表的名字和字段的名字，规范来说最好加上`号。

**引号**：内容都是字符串，一定要加上引号。不加上引号的话，MySQL就无法正确的识别为字符串，也会报错的。当然，如果是数字的话，就不用打引号了。

```sql
-- 多行插入
INSERT INTO 表名 (列1, ...)
VALUES (值1, ...),
       (值1, ...)...;

INSERT INTO `Articles` (`title`, `content`)
VALUES ('将进酒', '天生我材必有用，千金散尽还复来。'),
       ('宣州谢朓楼饯别校书叔云', '抽刀断水水更流，举杯消愁愁更愁。'),
       ('梦游天姥吟留别', '安能摧眉折腰事权贵，使我不得开心颜！'),
       ('春夜宴从弟桃花园序', '天地者，万物之逆旅也；光阴者，百代之过客也。'),
       ('宣州谢朓楼饯别校书叔云', '弃我去者，昨日之日不可留；乱我心者，今日之日多烦忧。'),
       ('庐山谣寄卢侍御虚舟', '我本楚狂人，凤歌笑孔丘。手持绿玉杖，朝别黄鹤楼。'),
       ('行路难', '长风破浪会有时，直挂云帆济沧海'),
       ('将进酒', '人生得意须尽欢，莫使金樽空对月。天生我材必有用，千金散尽还复来。'),
       ('望庐山瀑布', '飞流直下三千尺，疑是银河落九天。'),
       ('访戴天山道士不遇', '树深时见鹿，溪午不闻钟。'),
       ('清平调', '云想衣裳花想容，春风拂槛露华浓。'),
       ('春夜洛城闻笛', '谁家玉笛暗飞声，散入春风满洛城。');

```

#### 修改语句

```sql
UPDATE 表名
SET 列1=值1,
    列2=值2, ...WHERE 条件

-- 例如：
UPDATE `Articles`
SET `title`='黄鹤楼送孟浩然之广陵',
    `content`='故人西辞黄鹤楼，烟花三月下扬州。'
WHERE `id` = 2;

```
#### 删除语句
```sql
DELETE
FROM 表名
WHERE 条件

-- 例如：
DELETE
FROM `ARTICLES`
WHERE `id` = 5;

```
